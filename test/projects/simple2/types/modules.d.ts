/** 图片引用 URL，文件名经过 hash 处理 */
declare module '*.jpg' {
    const ImagePath: string;
    export = ImagePath;
}
/** 图片引用 URL，文件名经过 hash 处理 */
declare module '*.jpeg' {
    const ImagePath: string;
    export = ImagePath;
}
/** 图片引用 URL，文件名经过 hash 处理 */
declare module '*.png' {
    const ImagePath: string;
    export = ImagePath;
}
/** 图片引用 URL，文件名经过 hash 处理 */
declare module '*.apng' {
    const ImagePath: string;
    export = ImagePath;
}
/** 图片引用 URL，文件名经过 hash 处理 */
declare module '*.gif' {
    const ImagePath: string;
    export = ImagePath;
}
/** SVG 引用 URL，文件名经过 hash 处理 */
declare module '*.svg' {
    const ImagePath: string;
    export = ImagePath;
}
