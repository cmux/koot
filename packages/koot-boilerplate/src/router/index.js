import routeModulesConfiguration from '@router/modules';

import { createReactRouterConfiguration } from 'koot-router';

const router = createReactRouterConfiguration(routeModulesConfiguration);

export default router;
